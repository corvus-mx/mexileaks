---
interface Props {
  showBackLink?: boolean;
}

const { showBackLink = false } = Astro.props;
---

<header class="max-w-4xl mx-auto px-4 pt-8 pb-4">
  {showBackLink && (
    <a href="/" class="inline-block text-corvus-silver hover:text-corvus-text font-semibold tracking-wide text-base mb-4 transition-colors">
      ← Portada
    </a>
  )}
  
  <div class="flex items-center gap-3 mb-4">
    <h1 class="text-4xl font-bold tracking-wider">
      <span class="text-corvus-text">CORVUS</span>
      <span class="text-corvus-red">🔥</span>
      <span class="text-corvus-text">MexiLeaks</span>
    </h1>
  </div>
  
  <p class="text-corvus-gray text-lg italic mb-4">
    Donde el valor ciudadano y la ética periodística se unen para proteger la verdad.
  </p>
</header>